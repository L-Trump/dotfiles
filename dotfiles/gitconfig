[user]
	{{@@ "name = " + user.name if user and user.name @@}}
	{{@@ "email = " + user.email if user and user.email @@}}
[credential]
	helper = cache
[gui]  
	encoding = utf-8  
[i18n]  
	commitencoding = utf-8  
[svn]  
	pathnameencoding = utf-8  
[core]
	quotepath = false 
{%@@ if git is defined and git.proxy is defined @@%}
[https]
	proxy = {{@@ git.proxy @@}}
[http]
	proxy = {{@@ git.proxy @@}}
{%@@ endif @@%}
